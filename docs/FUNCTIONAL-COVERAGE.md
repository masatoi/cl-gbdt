# Functional Coverage

Every symbol `cl-gbdt`, `cl-gbdt/lightgbm` and `cl-gbdt/xgboost` export has a position here:
either the functional suite's own sources name it, or a row below says why they do not.
`tools/ci/check-functional-coverage.lisp` fails the build when a published symbol has neither, so
a symbol cannot reach the public surface without the project taking a position on how it is
proven.

**Covered is not written down.** The checker derives it: a symbol is covered when it appears in
some top-level form of a `tests/functional/*.lisp` file, outside that file's own package form.
Recording it here as well would give it a second home to go stale in -- the same rule
`ffi-spec/BINDING-COVERAGE.md` follows for `wrapped`.

Neither are counts or percentages. Coverage in this project is guaranteed by classification --
every symbol has a recorded position -- not by a number. The checker prints the live counts on
every run.

**What being covered does NOT mean.** That the suite's source names a symbol is not that the
symbol's contract is exercised. A condition named in a `handler-case` clause counts. So does a
function called once inside a fixture nothing asserts on. This file records where every published
symbol stands; it does not measure how well anything is tested, and a green run is not evidence
that it does.

The heading a row sits under is part of what the checker reads. It recognises exactly two
spellings: the literal `## Unproven`, and any heading beginning `## Exempt`. A row under anything
else -- a renamed heading, a new one, or one preceding the file's first `## ` heading -- fails the
build rather than landing silently in either bucket.

Each `## Exempt` heading carries its section's argument, so a row's Note is for what the heading
does not already say. A symbol whose argument fits none of the headings belongs under
`## Unproven`. The heading vocabulary above is a prefix match, not a fixed list: a new heading
spelled `## Exempt: ...` is recognised the moment it is written -- prose alone, no change to the
checker at all. Adding a row under a heading that already exists has the identical effect and
needs no new heading at all: the checker asks only whether a row's section is `## Unproven` or
begins `## Exempt`, never whether the row's own argument matches the one its heading states.
What the checker actually guarantees is that no published symbol goes
unclassified; whether a given `## Exempt` heading -- new or existing -- is honestly the right
one for a given row is for a reviewer reading the prose to judge, not something this build step
can check.

Rows name the symbol the way `docs/API-REFERENCE.md` heads its entries -- qualified by the
shortest public package that exports it -- so a reader can move between the two files.

Notes are Markdown, so a code span closes with a backtick on both ends (`` `foo` ``); Lisp
source's `` `foo' `` convention -- an opening backtick and a closing apostrophe -- is not used
here, since an unclosed backtick leaves the rest of the row unrendered as code.

## Unproven

Symbols that should have a functional test and do not. This is the work list. Each Note says what
a test would have to do, which is what an order of work gets argued from.

| Symbol | What a functional test would have to prove |
|---|---|
| `cl-gbdt:version-range-verified-evidence` | `version-range-tested-description` reads this slot into every VERIFIED-labelled entry it formats, but no assertion in `tests/version.lisp` checks the evidence text a formatted entry carries -- `version-range-tested-description-distinguishes-verified-from-inferred` only greps for the low/high numbers, not the parenthesised evidence string. A test proving this needs nothing from a shared library: it only has to call `version-range-tested-description` (or the accessor directly) on a fixture with a known evidence string and assert that string appears in the result. |
| `cl-gbdt:version-range-inferred-evidence` | Same gap, on the INFERRED-labelled entry: `version-range-tested-description` reads this slot too, and no assertion checks the inferred evidence text it produces. Also provable at layer 1 with no library -- the same kind of direct assertion as above, on the second entry `version-range-tested-description` returns. |
| `cl-gbdt:*lightgbm-version-range*` | Read by nothing but its own docstring and `src/version.lisp` itself -- `clgrep-search` across `src/` finds it named only in `version.lisp`'s own definition and, in prose, inside `lightgbm/classes.lisp`'s `initialize-backend` docstring, never in that method's code. Its own docstring states why: LightGBM's C API has no version entry point, `backend-version` is always NIL there, and `check-backend-version` is never called for this backend, so nothing -- no functional test, no layer 1 test, no runtime code path at all -- can compare a real library against this range. It is recorded for documentation only; there is no behavior left to prove. |
| `cl-gbdt:*xgboost-version-range*` | Read only by `xgboost/classes.lisp`'s `initialize-backend`, which passes it to `check-backend-version` right after reading the just-opened library's real version -- so proving this variable's recorded bounds match the vendored library means opening the real XGBoost backend and asserting `open-backend :xgboost` (or an equivalent functional fixture) signals no `untested-backend-version` warning. No test today, layer 1 or functional, references `*xgboost-version-range*` by name, even though every functional test that opens the XGBoost backend exercises it at runtime. |
| `cl-gbdt:csr-matrix-indices` | Read by both backends' real code -- `lightgbm/api.lisp`'s and `xgboost/api.lisp`'s `%dataset-pointer`, and `xgboost/api.lisp`'s `predict` -- to pin a `csr-matrix`'s column indices before handing them to the library, and `tests/functional/sparse-input.lisp` builds real `csr-matrix` instances and predicts against both real backends, so a badly broken reader would likely fail those tests. But no test, layer 1 or functional, calls `csr-matrix-indices` and checks what it returns, or isolates INDICES from its three siblings (INDPTR, VALUES, NUM-COLUMNS) the way `csr-matrix-reports-its-shape` does for them: that test checks NUM-COLUMNS, NUM-ROWS, VALUES and INDPTR's type, and never mentions INDICES; `csr-matrix-slots-are-read-only` only confirms the symbol names a function with no `setf` writer. A test proving this needs nothing from a shared library: a fixture built with known indices, read back through `csr-matrix-indices` and compared with `equalp` -- the same shape `csr-matrix-reports-its-shape` already uses for VALUES -- would close the gap without opening either library. |
| `cl-gbdt:backend-library-load-failed-cause` | `backend-library-load-failed` is never caught in any functional test (see that condition's row below) and, at layer 1, `tests/library.lisp`'s `resolve-and-load-library-honors-explicit-path` proves the condition and reads `backend-library-load-failed-path` back, but never CAUSE -- nothing anywhere calls `backend-library-load-failed-cause` by name, and the test never even prints the report. CAUSE is "the condition `cffi:load-foreign-library` itself signalled, kept here rather than reported directly so its own message and type both survive" -- exactly the fact a caller diagnosing a load failure needs (a missing file, the wrong format, a permissions error, each a different CFFI condition). A test proving this needs no shared library: pointing `:path` at a file that exists but is not a valid shared object (unlike the current fixture's simply-nonexistent path) would give `cffi:load-foreign-library` something real to fail on, and the test could then assert CAUSE is a `condition` carrying a non-empty message. |
| `cl-gbdt:dimension-mismatch-expected` | `dimension-mismatch` is caught for real in functional tests -- `custom-objective.lisp`'s `a-wrong-shaped-gradient-signals-before-the-library-sees-it` provokes it twice, with a flat gradient and with a two-value return of the wrong rank, and `xgboost-api.lisp`'s `xgboost-api-evaluate-one-iteration-with-mismatched-names-signals-dimension-mismatch` provokes it with more names than datasets -- but neither reads EXPECTED back; both confirm only the condition type. Nothing anywhere calls `dimension-mismatch-expected` by name. A test proving this would read EXPECTED back after one of these provocations and assert its exact wording -- for the objective case, that it names `(rows groups)` as `check-objective-result`'s own docstring promises. |
| `cl-gbdt:dimension-mismatch-given` | Same gap as `dimension-mismatch-expected` (see that row): the same two functional tests provoke `dimension-mismatch` for real and read neither slot back. A test proving this would assert GIVEN holds the actual malformed shape supplied -- e.g. the flat gradient's dimensions in the custom-objective case, or the name/dataset count mismatch in the `evaluate-one-iteration` case. |
| `cl-gbdt:foreign-call-error-code` | `foreign-call-error` is named, in the fourteen non-standalone functional files, in eleven `deftest` forms across both backends, across fourteen separate catch clauses -- `sparse-input.lisp` alone contributes three tests and six of those clauses (a `:reference`-built valid set, an independently-binned one, and `max_bin=1`); the rest are a wrong-length label, an out-of-range `booster-eval` index, an unevaluable valid set, and a wrong-width `predict`, one clause each. Two of the fourteen are negative rather than caught for real -- `lightgbm-api.lisp:661` and `sparse-input.lisp:394` assert the error does NOT fire, `(cl-gbdt:foreign-call-error () nil)`, so neither ever executes on a green run -- leaving twelve that catch a genuine C-level failure. (A third `(cl-gbdt:foreign-call-error () nil)` exists, at `prediction-shape.lisp:407`, so grepping for that exact text finds three; it is not a negative assertion like the two above, though -- it is a capability probe over a `dolist` of `:kind`s, catching the error as "this kind is not served" rather than asserting it never fires, and for XGBoost's `:leaf-index`/`:contrib` it genuinely does fire, so this clause belongs among the twelve real catches, not the two negative ones.) No clause among the fourteen reads CODE back; one, `xgboost-api.lisp:1445`, binds `(c)` and reads `foreign-call-error-message` twice instead, the only one of the fourteen that reads anything off the condition at all. The suite's two `*-standalone.lisp` files sit outside this count: `xgboost-standalone.lisp` (lines 393 and 406, one more `deftest`) adds two further real catches, spelled `cl-gbdt/xgboost:foreign-call-error` -- `eq` to this symbol, condition types not being redefined per backend -- and `lightgbm-standalone.lisp` names the condition once more, but only in a comment, catching nothing. `foreign-call-error-code` is read back only at layer 1, by `tests/conditions.lisp`'s `foreign-call-error-carries-backend-message` and `tests/foreign.lisp`'s `check-foreign-call-signals-on-failure`, both against a fabricated condition or a fake thunk rather than a real LightGBM/XGBoost failure. A test proving this would provoke one of the real functional failures above and assert CODE is the actual nonzero status the library returned. |
| `cl-gbdt:foreign-call-error-function-name` | Same gap as `foreign-call-error-code` (see that row): caught for real repeatedly in functional tests, read back only at layer 1 against fabricated conditions. `xgboost-api.lisp`'s `xgboost-api-slice-model-rejects-an-interval-it-cannot-honour` comes closest, reading `foreign-call-error-message` (not one of this task's thirty) back from a real `XGBoosterSlice` failure, but no functional test reads FUNCTION-NAME. A test proving this would assert FUNCTION-NAME names the actual C entry point the failing call reached. |
| `cl-gbdt:missing-training-set-booster` | `missing-training-set` is caught for real in `xgboost-api.lisp`'s `xgboost-api-update-one-iteration-on-load-model-booster-signals-missing-training-set`, calling `update-one-iteration` on a `load-model` booster, but the test confirms only the type, `(cl-gbdt:missing-training-set () t)`; BOOSTER is never read. Nothing else, layer 1 or functional, references `missing-training-set-booster` by name. A test proving this would read BOOSTER back and assert `eq` identity with the `loaded` booster `update-one-iteration` was called on, confirming the condition names the actual culprit rather than some other object. |
| `cl-gbdt:unfreed-handle-warning-kind` | `unfreed-handle-warning` itself is proven at layer 1 without a library (see that condition's row below), but the proving test, `tests/handle.lisp`'s `finalizer-closure-reports-only-when-unreleased`, only checks whether the warning fires, `(cl-gbdt:unfreed-handle-warning () t)`, never reading KIND or the rendered report. Nothing anywhere calls `unfreed-handle-warning-kind` by name. A test proving this would call `%make-finalizer` with a known KIND -- `:dataset` or `:booster` -- provoke the warning the same way that test already does, and assert `unfreed-handle-warning-kind` returns exactly the keyword given -- what tells a caller auditing a leak which sort of handle it was. |
| `cl-gbdt:unknown-capability-capability` | `unknown-capability` itself is proven at layer 1 without a library (see that condition's row below), but the proving test, `tests/backend.lisp`'s `backend-supports-p-signals-on-an-unregistered-capability`, only checks whether the condition fires, `(cl-gbdt:unknown-capability () t)`, never reading CAPABILITY or the rendered report. Nothing anywhere calls `unknown-capability-capability` by name. A test proving this would provoke the same misspelled `:model-slcing` and assert CAPABILITY reads back as exactly that keyword -- the fact a caller needs to confirm which of their own arguments was rejected. |
| `cl-gbdt:unknown-capability-known` | Same gap as `unknown-capability-capability` (see that row): the same layer 1 test never reads KNOWN or the rendered report back, and nothing else does either. A test proving this would assert KNOWN comes back as `*known-capabilities*` itself, or at minimum contains every capability `backend-supports-p` actually recognizes -- what a caller correcting a typo would want to see. |
| `cl-gbdt:unsupported-argument-reason` | `unsupported-argument` is caught for real, in the fourteen non-standalone functional files, in 23 catch clauses spread over 8 files. Within the three this row draws on -- `categorical-features.lisp` (`a-bad-categorical-index-signals` and `the-parameters-key-is-refused`, 2 clauses), `custom-evaluation.lisp` (6 clauses) and `custom-objective.lisp` (1 clause), 9 clauses total -- both `(cl-gbdt:unsupported-argument-argument condition)` and `(cl-gbdt:unsupported-argument-backend condition)` are asserted every time. That is not the whole suite, though: the other 14 clauses, in `missing-value.lisp`, `lightgbm-api.lisp`, `early-stopping.lisp`, `training-report.lisp` and `xgboost-api.lisp`, mostly read neither slot back (`(cl-gbdt:unsupported-argument () t)`) -- 13 of the 14 read only ARGUMENT or neither; `missing-value.lisp`'s one clause reads both, like the three files above. REASON itself is read back nowhere in any of these 23, in any file. The `*-standalone.lisp` files add three more, all in `lightgbm-standalone.lisp` -- two in `layer-1-alone-saves-loads-and-renders-a-model` (lines 472 and 478) and one in `layer-1-alone-reports-importance-evaluation-and-shape` (line 579) -- spelled `cl-gbdt/lightgbm:unsupported-argument`; `xgboost-standalone.lisp` names it nowhere. A test proving this would extend one of the existing assertions -- most directly one of the nine in the three files above, already reading the other two slots -- with a check on REASON, e.g. that `%check-categorical-parameter-keys`'s refusal names which of the five `categorical_feature` spellings collided. |
| `cl-gbdt:untested-backend-version-backend` | `untested-backend-version` is referenced in exactly one functional test, `xgboost-api.lisp`'s `xgboost-api-open-backend-against-vendored-library-warns-nothing`, which `handler-bind`s it around a real `open-backend :xgboost` specifically to assert the warning does NOT fire against the vendored library -- so no functional run ever actually catches a live instance to read BACKEND from. The only place BACKEND is read back is `tests/version.lisp`'s `check-backend-version-warns-below-the-inferred-range`, calling `check-backend-version` directly against a fixture range rather than through a real `initialize-backend`. A test proving this from a functional run would mean opening XGBoost with `*xgboost-version-range*` deliberately narrowed (or an out-of-range version otherwise forced) and reading BACKEND back from the condition that fires for real, confirming it names `:xgboost` rather than a fixture value. |
| `cl-gbdt:untested-backend-version-tested` | Same gap as `untested-backend-version-backend` (see that row): the one functional reference only asserts the warning's absence, never its content, and TESTED is read back only against `tests/version.lisp`'s fixture range. A test proving this from a functional run would assert TESTED equals `(version-range-tested-description *xgboost-version-range*)` -- the real, shipped range's own description -- rather than a fixture's. |
| `cl-gbdt:untested-backend-version-version` | Same gap again (see the two rows above): the one functional reference only asserts absence, and VERSION is read back only against a fixture. This is the slot a caller most needs -- the actual out-of-range (or unparseable, or NIL) version string that was loaded -- and a test proving it from a functional run would need to force `initialize-backend` to see a version outside `*xgboost-version-range*` and confirm VERSION carries that real string back. |
| `cl-gbdt:wrong-backend-reference-argument` | `wrong-backend-reference` is caught for real in sixteen functional tests in the fourteen non-standalone functional files, across both backends -- a booster passed as `:reference`, a booster as `train`'s dataset, another backend's handle at nearly every Layer 1 entry point -- more than any other condition in this task. A handful read the rendered report back, but only to confirm two fixed phrases, `"must be a dataset built by"` and `"must be a booster built by"` (`lightgbm-api.lisp`'s `lightgbm-api-train-with-a-booster-in-valid-sets-signals` and `lightgbm-api-booster-eval-with-a-dataset-as-booster-argument-signals`, and their XGBoost equivalents in `xgboost-api.lisp`), which pins EXPECTED's wording (see that row below) but nothing about ARGUMENT -- no test anywhere checks that the report names the specific argument that was wrong (`"create-booster's dataset argument"`, `"a create-booster :valid-sets entry"`, and the rest all differ per call site). The two `*-standalone.lisp` files add four `deftest` forms each -- eight more -- and 26 more catch clauses, thirteen per file, spelled `cl-gbdt/lightgbm:wrong-backend-reference` and `cl-gbdt/xgboost:wrong-backend-reference`; none of those reads ARGUMENT back either. A test proving this would read ARGUMENT back directly and assert it names the actual call site. |
| `cl-gbdt:wrong-backend-reference-backend` | Same breadth of real catches as `wrong-backend-reference-argument` (see that row), and the same gap: no functional test reads BACKEND back or checks that the backend's own name appears in the report. A test proving this would assert BACKEND is exactly the backend the operation was called against -- distinct from `backend-error-backend`, the unrelated slot of the same name on a different condition family that IS read directly throughout the functional suite. |
| `cl-gbdt:wrong-backend-reference-expected` | The most nearly-proven of the four `wrong-backend-reference` readers: several functional tests search the rendered report for `"must be a dataset built by"` or `"must be a booster built by"`, which only holds if EXPECTED came back as `"dataset"` or `"booster"` respectively, and both values are exercised (`lightgbm-api-train-with-a-booster-in-valid-sets-signals` for the default `"dataset"`, `lightgbm-api-booster-eval-with-a-dataset-as-booster-argument-signals` for the `"booster"` `booster-eval`/`booster-eval-names` pass). But no test calls `wrong-backend-reference-expected` directly, or checks the third value, `"backend"`, at all -- every `create-dataset`/`create-booster`/`load-model` backend-argument test in `lightgbm-api-layer-1-refuses-the-other-backends-backend` and its XGBoost mirror confirms only the condition type. A test proving this fully would read EXPECTED back directly in all three cases, including `"backend"`. |
| `cl-gbdt:wrong-backend-reference-given` | Same breadth of real catches as the other three `wrong-backend-reference` readers (see those rows). GIVEN is the class of the actually-wrong object, and no functional test reads it back or checks the report's `"not ~A"` clause for the specific class name it should carry (e.g. `"XGBOOST-BOOSTER"`). A test proving this would read GIVEN back and assert it identifies the actual offending object's class or type. |
| `cl-gbdt:handle` | The shared supertype `dataset` and `booster` both inherit from, holding the pointer, released-cell and backend slots and the finalizer machinery once rather than twice -- its own docstring says "Build instances with `make-handle`, never directly." Nothing anywhere breaks that promise: `clgrep-search` for `make-handle`'s CLASS-NAME argument across `src/` and `tests/` finds only `dataset`, `booster`, `lightgbm-dataset`, `lightgbm-booster`, `xgboost-dataset` and `xgboost-booster` -- never the bare `handle` symbol. No `defmethod` anywhere specializes on it either: every handle-dispatching method in `src/protocol.lisp` (`dataset-num-rows`, `predict`, `free-booster` and the rest) and every backend override specializes on `dataset` or `booster` specifically. `cl-gbdt:dataset` is `typep`-tested directly by name in `tests/handle-ownership.lisp` (see that row below); nothing takes that one step further up to the class both `dataset` and `booster` actually share. A test proving this would build one of each concrete kind through `make-handle` and assert `(typep x 'cl-gbdt:handle)` on both -- the same shape of evidence `tests/handle-ownership.lisp` already gives `dataset` alone. |
| `cl-gbdt/lightgbm:lightgbm-backend` | The concrete class `(open-backend :lightgbm)` returns a real instance of on every functional run -- not a stand-in, the same class every LightGBM `defmethod` in `lightgbm/classes.lisp`, `lightgbm/protocol.lisp` and `lightgbm/api.lisp` specializes on. No test, layer 1 or functional, asserts anything about it by name: `clgrep-search` for `lightgbm-backend` across `tests/` returns nothing at all. Unlike `with-pointer-ownership`, whose own docstring disclaims caller use, this class's docstring is caller-facing prose -- "A connection to the LightGBM shared library, implementing cl-gbdt's unified backend protocol" -- and `docs/API-REFERENCE.md` gives it its own entry. `dataset` and `booster` are the same shape (opaquely returned, never named by a caller) but went to the evidence-based section above because a layer 1 test actually names and `typep`s them; no test anywhere does that for this class, so the same evidence is simply absent here rather than substitutable by a different argument. A test proving this needs nothing beyond what any functional fixture already does: `(ok (typep (cl-gbdt:open-backend :lightgbm) 'cl-gbdt/lightgbm:lightgbm-backend))`, one line added to an existing `with-backend-library` block. |
| `cl-gbdt/xgboost:xgboost-backend` | Same gap as `lightgbm-backend` above, for the other backend: `(open-backend :xgboost)` returns a real instance on every functional run, `clgrep-search` for `xgboost-backend` across `tests/` returns nothing, and its docstring is the same caller-facing shape -- "A connection to the XGBoost shared library, implementing cl-gbdt's unified backend protocol." A test proving this is the same one-line `typep` addition to an existing fixture. |
| `cl-gbdt:backend-version` | XGBoost's `initialize-backend` (`src/xgboost/classes.lisp`) reads the real, just-loaded library's own version through `%read-version` and records it, `(setf (backend-version backend) (%read-version))`, then immediately passes it to `check-backend-version` -- both run for real on every functional test that opens `:xgboost`. LightGBM's `initialize-backend` always sets it to NIL instead, since "LightGBM's C API has no runtime version query" (see `*lightgbm-version-range*`'s row above). But no functional test reads `cl-gbdt:backend-version` back by name: `xgboost-api.lisp`'s `xgboost-api-open-backend-against-vendored-library-warns-nothing` only confirms `untested-backend-version` does not fire, which proves the range comparison, not the accessor. `tests/backend.lisp`'s `backend-info-reports-capabilities-and-version` reads it back at layer 1, but only the mock backend's own hand-set `"0.0.0-mock"`, through `backend-info`, never a real library's own account of itself. A test proving this would open each real backend and read `(cl-gbdt:backend-version backend)` back directly: a non-empty string parsing as `MAJOR.MINOR.PATCH` for XGBoost, and NIL for LightGBM -- confirming the documented asymmetry for real rather than by reading the docstring alone. |
| `cl-gbdt:backend-info` | No functional test calls `cl-gbdt:backend-info` by name -- `clgrep-search` across `tests/functional/` finds nothing. `tests/backend.lisp`'s `backend-info-reports-capabilities-and-version` proves the aggregation at layer 1, against the `:mock` backend: it reads back `:name`, `:version`, `:capabilities` and `:open`, but never `:library-path` -- the mock's `initialize-backend` never sets it, so the plist's fifth key goes unchecked even there -- and the four it does check are the mock's own hand-set fiction (`"0.0.0-mock"`, `'(:mock t)`), never a real library's own report. A test proving this would call `(cl-gbdt:backend-info backend)` on a really-opened `:lightgbm` or `:xgboost` backend and assert all five keys carry what the real library actually reports: a real capabilities plist, `backend-version`'s real (or, for LightGBM, genuinely NIL) string, and `backend-library-path`'s own gap (see that row below) read back through this same call. |
| `cl-gbdt:backend-library-path` | `tests/functional/support.lisp` defines its own, unrelated `backend-library-path` (exported from `cl-gbdt/tests/functional/support`, not `cl-gbdt`) -- it takes a backend KEYWORD (`:lightgbm`, `:xgboost`) and computes the vendored file's path from the environment and the filesystem *before* any load, for `with-backend-library` to hand `cffi:load-foreign-library`. Four functional tests call that function, not this one, six times between them: `functional/lightgbm.lisp`'s `lightgbm-library-loads` (once) and `lightgbm-default-library-name-resolves-through-cffi-default` (twice), and `functional/xgboost.lisp`'s `xgboost-library-loads` (once) and `xgboost-default-library-name-resolves-through-cffi-default` (twice). `cl-gbdt:backend-library-path` -- the class accessor, taking a backend INSTANCE, set only inside `initialize-backend` after `resolve-and-load-library` actually succeeds -- is never called by any functional test; the two symbols share a name but nothing else. A test proving this would read `(cl-gbdt:backend-library-path backend)` back after a real `open-backend` and assert it names the same file `support:backend-library-path` independently resolved -- the one fact the accessor exists to report and no test currently checks. |
| `cl-gbdt:probe-capabilities` | Called four times across three tests at layer 1, in `tests/backend.lisp`: `probe-capabilities-reports-every-declared-capability` and `probe-capabilities-does-not-signal-for-a-missing-symbol` (one call each, both against the fabricated symbol name `"ThisSymbolDoesNotExistAnywhere"`), and `probe-capabilities-records-a-provided-capability-as-true` (two calls -- the first against that same fabricated name plus `:provided '(:evaluation-history)`, the second against an empty optional-symbols list with the same `:provided`). None resolves a real library's actual optional symbols -- proving that an unresolvable name is present and false rather than omitted, that a missing optional symbol signals nothing, and that a `:provided` name reads true needing no resolvable symbol of its own -- but never the real answer this project ships: whether the vendored LightGBM and XGBoost libraries actually resolve `*optional-symbols*`'s real C names. The two-call test's second assertion proves only that `:evaluation-history` in `:provided` leaves `:model-slicing` in the probed alist unaffected -- a different-name check, not the same-name precedence the docstring separately claims (PROVIDED's entries come first, so a capability named in both PROVIDED and OPTIONAL-SYMBOLS reads true through `getf` whatever the probe found); no test puts one capability name in both lists to confirm that precedence for real. Both backends' `initialize-backend` call it for real, with the real vendored library, `*optional-symbols*` and `*provided-capabilities*`, on every functional test that opens a backend -- but nothing there reads its return value back directly; the result only flows on into `backend-capabilities`, read afterward through `backend-supports-p` (see `*known-capabilities*`'s row under "Exempt: reached through a published wrapper"), never through `probe-capabilities` itself. A test proving this would call `(cl-gbdt:probe-capabilities *optional-symbols* :provided *provided-capabilities* :library library)` directly, against a really-loaded vendored library, and assert the plist it returns matches what `backend-supports-p` reports for the same backend afterward -- closing the gap between the proven mechanism and the real data. |
| `cl-gbdt:file-format-mismatch` | Published by the file-input-layer-1 branch's Task 2 as the condition the pure layer's gate signals, ahead of Task 3/4's `create-dataset-from-file`, which is what will actually raise it -- that operation does not exist yet, so no `error` naming this condition appears anywhere in the tree, functional or layer 1. `tests/xgboost-file-input.lisp` exercises the two functions this gate is built from, `detect-file-format` and `file-uri`, directly, but neither one signals `file-format-mismatch` itself -- `file-uri`'s own guard signals `unsupported-argument` instead. A test proving this would exist once `create-dataset-from-file` lands: declare a `:format` that disagrees with a fixture file's real contents (e.g. `:libsvm` on a real CSV file) and confirm the condition fires before any foreign call, rather than the library ever being reached -- the crash this condition exists to prevent, per its own docstring. |
| `cl-gbdt:file-format-mismatch-declared` | Same gap as the condition itself (see that row): nothing anywhere constructs a `file-format-mismatch`, so none of its three readers is called either. A test proving this would read DECLARED back from a provoked instance and assert it echoes the caller's own `:format` argument to `create-dataset-from-file` unchanged. |
| `cl-gbdt:file-format-mismatch-detected` | Same gap (see `file-format-mismatch`'s row). A test proving this would read DETECTED back and assert it names what `detect-file-format` actually classified the fixture as -- `:csv` for a CSV file wrongly declared `:libsvm`, the same classification this branch's layer 1 tests already prove `detect-file-format` gives independently. |
| `cl-gbdt:file-format-mismatch-path` | Same gap (see `file-format-mismatch`'s row). A test proving this would read PATH back and assert it names the fixture file that was declared wrong. |

## Exempt: proven without a shared library

The symbol's whole contract is computation, or CFFI/SBCL machinery, that neither vendored
library takes part in, and the layer 1 suite proves it. A functional test here would open a
library, ignore it, and assert what `tests/*.lisp` already asserts -- three minutes of CI for
no additional evidence.

A row belongs here only when the layer 1 test exists. "Nothing calls the library" is half the
argument; the Note carries the other half, by naming the test that does the proving.

| Symbol | What proves it instead |
|---|---|
| `cl-gbdt:version-compare` | Orders two version strings and touches nothing else. `tests/version.lisp` covers each ordering, equal versions, differing component counts, and both malformed operands, in `version-compare-orders-by-component` and `version-compare-rejects-unparseable-input`. |
| `cl-gbdt:version-in-range-p` | Compares a version against two bounds via `version-compare` alone. `tests/version.lisp`'s `version-in-range-p-covers-inside-and-both-boundaries` proves both inclusive boundaries and a single-point range; `version-in-range-p-covers-below-and-above` proves both out-of-range directions; `version-in-range-p-cannot-confirm-unparseable-or-nil` proves an unparseable or NIL version or bound returns NIL rather than a false confirmation. |
| `cl-gbdt:check-backend-version` | Compares a version string against a range's *inferred* bounds and conditionally `warn`s; never touches a library itself, only ever called (at runtime) by XGBoost's `initialize-backend`. `tests/version.lisp`'s five `check-backend-version-*` tests, using a fixture range and `handler-bind`-collected warnings, prove: no warning inside the inferred range including both boundaries (`check-backend-version-does-not-warn-inside-the-inferred-range`); a warning below it, carrying the right backend, version and tested-description (`check-backend-version-warns-below-the-inferred-range`); a warning above it (`check-backend-version-warns-above-the-inferred-range`); a warning on an unparseable version (`check-backend-version-warns-on-unparseable-version`); and a warning on a NIL version (`check-backend-version-warns-on-nil-version`). |
| `cl-gbdt:make-version-range` | Constructs a `version-range` record from its six keyword slots. Six `deftest` forms in `tests/version.lisp` actually call it: five reach it indirectly through the `%fixture-range` helper (`check-backend-version-does-not-warn-inside-the-inferred-range`, `check-backend-version-warns-below-the-inferred-range`, `check-backend-version-warns-above-the-inferred-range`, `check-backend-version-warns-on-unparseable-version`, `check-backend-version-warns-on-nil-version`), and `version-range-tested-description-distinguishes-verified-from-inferred` calls it both through that same helper and again directly, to build a second range with distinct VERIFIED bounds. A keyword constructor does not depend on argument order; what these tests would actually catch is a renamed or dropped keyword. |
| `cl-gbdt:version-range` | The record type `make-version-range` builds; every instance of it in `tests/version.lisp` flows only into `check-backend-version` or `version-range-tested-description`, never anything else. Proven by the same six tests as `make-version-range` (see that row) -- the five `check-backend-version-*` tests via `%fixture-range`, plus `version-range-tested-description-distinguishes-verified-from-inferred`, which also builds a second instance directly -- both construct and consume an instance with exactly its six documented slots. |
| `cl-gbdt:version-range-verified-low` | Read into the VERIFIED-labelled entry `version-range-tested-description` formats. `version-range-tested-description-distinguishes-verified-from-inferred`'s first case uses a fixture with VERIFIED-LOW = VERIFIED-HIGH = `"3.3.0"` and asserts the formatted entry contains `"3.3.0 verified"`; its second case uses VERIFIED-LOW = `"4.0.0"` (distinct from VERIFIED-HIGH) and asserts the entry contains `"4.0.0-4.7.0 verified"`, which only holds if this accessor returns exactly `"4.0.0"`. |
| `cl-gbdt:version-range-verified-high` | Same mechanism as `version-range-verified-low`, proven by the same two cases in `version-range-tested-description-distinguishes-verified-from-inferred`: the first case's fixture sets VERIFIED-HIGH equal to VERIFIED-LOW and asserts the dash is omitted (`(ng (search "3.3.0-3.3.0" ...))`); the second sets VERIFIED-HIGH = `"4.7.0"`, distinct from VERIFIED-LOW, and asserts the entry contains `"4.0.0-4.7.0 verified"`, which only holds if this accessor returns exactly `"4.7.0"`. |
| `cl-gbdt:version-range-inferred-low` | Read into the INFERRED-labelled entry `version-range-tested-description` formats. `version-range-tested-description-distinguishes-verified-from-inferred`'s first case uses a fixture with INFERRED-LOW = `"1.7.0"`, INFERRED-HIGH = `"3.3.0"`, and asserts the second formatted entry contains `"1.7.0-3.3.0 inferred"`, which only holds if this accessor returns exactly `"1.7.0"`. |
| `cl-gbdt:version-range-inferred-high` | Same case as `version-range-inferred-low`: `version-range-tested-description-distinguishes-verified-from-inferred`'s assertion that the second formatted entry contains `"1.7.0-3.3.0 inferred"` only holds if this accessor returns exactly `"3.3.0"`, the fixture's INFERRED-HIGH. |
| `cl-gbdt:version-range-tested-description` | Formats a range's VERIFIED and INFERRED bounds as two evidence strings. `tests/version.lisp`'s `version-range-tested-description-distinguishes-verified-from-inferred` proves the no-dash case (VERIFIED-LOW = VERIFIED-HIGH) and the dashed case (VERIFIED-LOW /= VERIFIED-HIGH); `check-backend-version-warns-below-the-inferred-range` proves its return value is exactly what `untested-backend-version`'s `:tested` initarg receives when `check-backend-version` warns. |
| `cl-gbdt:normalize-parameters` | Turns a keyword plist into backend key/value string pairs and touches nothing else. `tests/parameters.lisp` covers name conversion to snake_case (`normalize-parameters-returns-name-value-pairs`), the odd-length-plist error (`normalize-parameters-rejects-odd-length-plist`), double-float, single-float and small-double-float printing without a type marker (`normalize-parameters-prints-double-float-without-a-type-marker`, `-single-float-`, `-small-double-float-`), ratio-to-decimal conversion (`normalize-parameters-prints-a-ratio-as-a-bare-decimal`, `-prints-a-ratio-as-a-decimal`), T/NIL as `"true"`/`"false"` (`normalize-parameters-prints-booleans-as-true-and-false`), and independence from the caller's `*print-base*` (`normalize-parameters-output-is-independent-of-the-caller-s-print-base`). |
| `cl-gbdt:csr-matrix` | The record type `make-csr-matrix` builds; `tests/data.lisp`'s CSR suite builds every instance through `%csr`, a thin wrapper over `make-csr-matrix`, and every instance flows only into this type's own readers, never into either library. Fourteen `deftest` forms exercise it: `csr-matrix-reports-its-shape` builds one and reads several slots back (see the individual reader rows below); nine `csr-matrix-rejects-*` tests and `csr-matrix-accepts-any-sequence` build and validate at construction time; `csr-matrix-allows-an-all-zero-trailing-column` and `csr-matrix-allows-an-empty-row` build and read back two more legal shapes; `csr-matrix-slots-are-read-only` confirms all four accessors exist with no `setf` writer. |
| `cl-gbdt:csr-matrix-indptr` | `csr-matrix-reports-its-shape` asserts its return value is coerced to exactly the `(simple-array (signed-byte 32) (*))` type `%coerce-index-vector` promises. Its LENGTH -- what `csr-matrix-num-rows` derives from it -- is proven for three different INDPTR inputs by that same test (length 3 -> 2 rows), `csr-matrix-accepts-any-sequence` (a vector INDPTR of length 3 -> 2 rows), and `csr-matrix-allows-an-empty-row` (length 4 with one repeated entry -> 3 rows). No test asserts the individual elements survive `make-csr-matrix`'s coercion unchanged -- only the type, and the length `csr-matrix-num-rows` depends on. |
| `cl-gbdt:csr-matrix-values` | `csr-matrix-reports-its-shape` asserts both its exact content -- `(equalp #(1.0d0 2.0d0 3.0d0) (cl-gbdt:csr-matrix-values m))` against the VALUES `%csr`'s default fixture builds -- and that it was coerced to `(simple-array double-float (*))`. |
| `cl-gbdt:csr-matrix-num-columns` | Asserted directly in two tests: `csr-matrix-reports-its-shape` (`(= 3 (cl-gbdt:csr-matrix-num-columns m))` against the default fixture's NUM-COLUMNS of 3) and `csr-matrix-allows-an-all-zero-trailing-column` (`(= 3 ...)` after building with an INDICES sequence that never reaches column 2, proving the declared width comes back rather than one inferred from the stored indices). |
| `cl-gbdt:csr-matrix-num-rows` | Asserted directly in three tests, each against a different INDPTR length: `csr-matrix-reports-its-shape` (`(= 2 ...)`, INDPTR length 3), `csr-matrix-accepts-any-sequence` (`(= 2 ...)`, a vector INDPTR of length 3), and `csr-matrix-allows-an-empty-row` (`(= 3 ...)`, INDPTR length 4 with a repeated entry for the empty row). |
| `cl-gbdt:foreign-element-type` | Maps `double-float`/`single-float` to `:double`/`:float` and touches nothing else. `foreign-element-type-maps-to-cffi-keywords` asserts both mappings directly; the `unsupported-element-type` condition it signals for anything else is exercised one layer up, through `with-foreign-matrix`, by `unsupported-element-type-is-rejected`. |
| `cl-gbdt:foreign-matrix` | The class `call-with-foreign-matrix` has a passthrough method for -- per that method's own docstring, "MATRIX is already foreign memory ... FUNCTION receives MATRIX's own pointer, dimensions and element type unchanged, with no copy and no pin." `foreign-matrix-is-passed-through` builds one via `make-instance` over a `cffi:with-foreign-object` buffer it owns, hands it to `with-foreign-matrix`, and asserts the pointer that comes back is `cffi:pointer-eq` to its own buffer -- proving no copy happened -- alongside both dimensions and the element type: the whole class exercised with neither library involved. |
| `cl-gbdt:foreign-matrix-pointer` | Read by `call-with-foreign-matrix`'s `foreign-matrix` method to hand FUNCTION the stored pointer unchanged. `foreign-matrix-is-passed-through` asserts the pointer `with-foreign-matrix` binds is `cffi:pointer-eq` to the exact buffer the instance was constructed with, which only holds if this reader returns it unchanged. |
| `cl-gbdt:foreign-matrix-rows` | Same mechanism and test as `foreign-matrix-pointer`: `foreign-matrix-is-passed-through` constructs an instance with `:rows 2` and asserts the NROW `with-foreign-matrix` binds is `2`, which only holds if this reader returns exactly what was stored. |
| `cl-gbdt:foreign-matrix-cols` | Same mechanism as `foreign-matrix-rows`: `foreign-matrix-is-passed-through` constructs an instance with `:cols 2` and asserts the NCOL `with-foreign-matrix` binds is `2`. |
| `cl-gbdt:foreign-matrix-element-type` | Same mechanism again: `foreign-matrix-is-passed-through` constructs an instance with `:element-type 'double-float` and asserts the ELEMENT-TYPE `with-foreign-matrix` binds is `eq` to `'double-float`. |
| `cl-gbdt:call-with-foreign-matrix` | Neither backend calls this generic function by name -- both reach it only through the `with-foreign-matrix` macro, which is what `tests/data.lisp` calls directly in every one of its eight matrix-handling tests, proving both defined methods and the branch inside the `array` one: `double-matrix-is-passed-row-major`, `single-matrix-is-passed-row-major` and `large-matrix-round-trips` exercise the SBCL pinned path over a `simple-array`; `non-simple-array-is-copied` exercises the copy fallback over an adjustable array; `pinned-matrix-survives-garbage-collection` proves the pinned pointer survives a full GC while held; `unsupported-element-type-is-rejected` and `wrong-rank-is-rejected` prove its two validation errors; `foreign-matrix-is-passed-through` proves the `foreign-matrix` method (see that row above). None of this reaches either shared library -- it is CFFI/SBCL memory management that runs identically whether or not LightGBM or XGBoost is even installed. |
| `cl-gbdt:check-foreign-call` | Never calls a library itself: CODE is a plain integer and LAST-ERROR is an opaque thunk supplied by the caller, the same shape as `check-backend-version` above -- it "never touches a library itself, only ever called (at runtime) by" each backend's own `check-lgbm`/`check-xgb` wrapper. Three `deftest` forms in `tests/foreign.lisp` supply fake thunks and prove the whole contract directly: `check-foreign-call-returns-the-code-on-success` proves a zero CODE returns it and never calls LAST-ERROR; `check-foreign-call-signals-on-failure` proves a nonzero CODE signals `foreign-call-error` carrying FUNCTION-NAME, CODE and LAST-ERROR's result; `check-foreign-call-tolerates-a-nil-message` proves a LAST-ERROR returning NIL still produces a report -- `"(no message)"` -- rather than a broken one. |
| `cl-gbdt:data-error` | Not abstract -- `normalize-parameters` in `src/parameters.lisp` signals it directly, `(error 'data-error)`, for an odd-length `:parameters` plist, reachable from any `make-dataset`/`train`/`create-booster` call since both backends' parameter renderers (`%parameter-string`, `%set-parameters`) call it. `data-error` has no slots, so there is nothing beyond "was it signalled" to prove, and that is exactly what `tests/parameters.lisp`'s `normalize-parameters-rejects-odd-length-plist` already does -- the same test the `cl-gbdt:normalize-parameters` row above cites -- calling `normalize-parameters` directly with `'(:objective)` and catching `data-error` itself, not a subclass. `normalize-parameters` touches no library; a functional `make-dataset` call with the same bad plist would reach the identical signal through a longer path and prove nothing more. |
| `cl-gbdt:backend-library-load-failed` | Signalled only from `src/library.lisp`'s `%load-candidate`, when `cffi:load-foreign-library` rejects a candidate path. No functional test provokes it -- every `tests/functional/*.lisp` fixture that opens a backend does so against the real vendored library, which loads successfully, so this state is unreached there by construction. `tests/library.lisp`'s `resolve-and-load-library-honors-explicit-path` provokes the real signal path directly, handing `resolve-and-load-library` an explicit nonexistent `:path` so `%load-candidate` signals for real, and reads `backend-library-load-failed-path` (see that row below) back -- proven, deliberately, "without ever successfully loading a real shared library into the image", per the file's own header comment. |
| `cl-gbdt:backend-methods-not-loaded` | Signalled from every fallback method in `src/protocol.lisp` (`make-dataset`, `train`, `load-model`, and the rest, specialized on the bare `backend`/`dataset`/`booster` classes) when a backend's unified-API methods were never loaded. Unreachable from any functional test: the aggregation system `cl-gbdt/tests/functional` (`cl-gbdt.asd`) depends on both backends' `.../unified` systems, so every one of its 16 files runs with the fallback method inapplicable -- for 13 of those files because they call `cl-gbdt:train` and friends and get the real, backend-specific method, and for a 14th, `tests/functional/support.lisp`, vacuously: it defines only fixtures and helpers, calls no unified generic itself, and names `cl-gbdt:predict` and `cl-gbdt:csr-matrix` nowhere but inside its own docstrings, which this checker's reader-based `functional-references` does not walk into; for the remaining two, `lightgbm-standalone.lisp` and `xgboost-standalone.lisp`, because each depends on `cl-gbdt/lightgbm`/`cl-gbdt/xgboost` alone (Layer 1, no `cl-gbdt`, no `.../unified` -- their own header comments say so, and `tools/ci/check-leaf-systems.lisp` enforces it) and so never calls a unified generic at all. `tests/protocol.lisp` -- whose own header states "these need no shared library (layer 1)" -- proves the whole contract against a bare `cl-gbdt:backend` instance that never calls `initialize-backend`: `backend-methods-not-loaded-is-a-backend-error` proves the hierarchy, `a-backend-dispatching-generic-says-which-system-to-load` and `make-dataset-and-load-model-signal-it-too` prove every backend-dispatching generic signals it, and `a-handle-dispatching-generic-names-the-handles-backend` proves a handle-dispatching generic does too. |
| `cl-gbdt:unfreed-handle-warning` | Signalled only from the closure `%make-finalizer` returns, fired by SBCL's finalizer machinery when a `dataset` or `booster` is garbage-collected still holding a live pointer. No functional test provokes it -- every functional fixture frees its handles explicitly or through `with-dataset`/`with-booster`, and provoking this for real would mean forcing a collection and depending on GC timing, which `tests/handle.lisp`'s own comment calls out as a source of flakiness. `finalizer-closure-reports-only-when-unreleased` proves the whole contract instead by invoking the finalizer closure directly -- "the exact closure a real handle carries" -- with a controlled RELEASED cell, no collection and no library needed: warns when unreleased, stays silent when released. |
| `cl-gbdt:unknown-backend` | Signalled from `open-backend` in `src/backend.lisp` before any library is touched, when the requested name has no registered class. Unreached by any functional test: every `tests/functional/*.lisp` fixture opens `:lightgbm` or `:xgboost`, both registered the moment their system loads. `tests/backend.lisp` -- whose own header states "a mock backend is used, so no shared library is required" -- proves it with `open-backend-rejects-unknown-name`, which opens `:no-such-backend` against a registry holding only `:mock` and `:failing` and catches `unknown-backend`, then reads the report back and finds both `"NO-SUCH-BACKEND"` and `"MOCK"` in it -- the second only appearing if `unknown-backend-registered` (see that row below) returned the registered list. |
| `cl-gbdt:unknown-capability` | Signalled from `backend-supports-p` in `src/backend.lisp` when the capability keyword is not a member of `*known-capabilities*`. Unreached by any functional test: every functional call to `backend-supports-p` names a real capability (`:sparse-input`, `:missing-value`, and the rest). `tests/backend.lisp`'s `backend-supports-p-signals-on-an-unregistered-capability` proves it against a `:mock` backend that opens no library, passing the deliberately misspelled `:model-slcing` and catching `unknown-capability`. |
| `cl-gbdt:backend-library-load-failed-path` | `tests/library.lisp`'s `resolve-and-load-library-honors-explicit-path` reads it directly: after an explicit `:path` of `"/nonexistent/cl-gbdt-test-path.so"` fails to load, `(cl-gbdt:backend-library-load-failed-path condition)` is asserted `equal` to that exact string -- the same layer 1 test, needing no real library, that proves the condition itself (see the row above). |
| `cl-gbdt:backend-library-not-found-searched` | Two `tests/library.lisp` tests read it directly. `resolve-and-load-library-env-var-is-strict` sets a fake `:env-var` to a nonexistent path and asserts SEARCHED is exactly `("/nonexistent/cl-gbdt-test-library.so")`, proving a strict `:env-var` failure does not fall through to report the directory or system search too. `resolve-and-load-library-falls-through-an-absent-directory` asserts SEARCHED has both entries when neither `:path` nor `:env-var` is given: the directory search and the string `"system library search path"`. Neither test loads a real library. `tests/functional/{lightgbm,xgboost}-standalone.lisp`'s `with-open-backend` also catches `backend-library-not-found` -- as a `rove:skip` for a fresh clone with no vendored library -- but only passes the condition to `princ-to-string` for a skip message; nothing there asserts on SEARCHED, so it adds no evidence beyond the two layer 1 tests. |
| `cl-gbdt:backend-methods-not-loaded-generic-function` | `tests/protocol.lisp`'s `a-backend-dispatching-generic-says-which-system-to-load` reads it directly: after `(cl-gbdt:train (bare-backend) nil)` signals, `(eq 'cl-gbdt:train (cl-gbdt:backend-methods-not-loaded-generic-function caught))` is asserted -- the same layer 1 test, over a bare backend that never opens a library, that proves the condition itself (see the row above). |
| `cl-gbdt:unknown-backend-registered` | `tests/backend.lisp`'s `open-backend-rejects-unknown-name` proves it indirectly through the rendered report: after `(cl-gbdt:open-backend :no-such-backend)` signals, the test asserts `(search "MOCK" text)` on the printed condition, which only holds if `unknown-backend-registered` returned the registered list -- the `:report` method's only source for it. `:mock` and `:failing` are `tests/backend.lisp`'s own registrations, but in the full layer 1 image REGISTERED also carries `:handle-test` and `:other-handle-test` (`tests/handle.lisp` registers both against the same process-wide `*backend-classes*`) -- more than the two names this row originally claimed, though `"MOCK"` still only appears if REGISTERED came back correctly, so the inference is unaffected. No library is loaded either way. |
| `cl-gbdt:dataset` | The class every training or validation handle is an instance of -- `create-dataset` and `make-dataset` return one on both backends, and every functional test holds one, but none ever writes the class name; a caller gets it back opaquely. Its contract as a `typep`/dispatch target is proven directly at layer 1, without either shared library: `tests/handle-ownership.lisp` builds one with `(with-pointer-ownership (...) (take-ownership 'cl-gbdt:dataset ...))` and asserts `(typep handle 'cl-gbdt:dataset)` three times (`taking-ownership-suppresses-the-free`, `the-bodys-values-are-the-forms-values`, `a-signal-after-ownership-moved-does-not-free`); `tests/handle.lisp` builds a bare `cl-gbdt:dataset` directly with `cl-gbdt:make-handle` in five more tests (`release-handle-frees-exactly-once`, `release-handle-second-call-is-a-no-op`, `live-pointer-on-released-handle-signals`, `live-pointer-on-live-handle-returns-the-pointer`, `live-pointer-on-handle-whose-backend-is-closed-signals-backend-not-open`) and again through its `%handle-on` helper for the `%check-handle-kind` tests; and `tests/protocol.lisp`'s `a-handle-dispatching-generic-names-the-handles-backend` builds one to prove a handle-dispatching generic reaches the right fallback. None of these opens a library -- a mock or bare `cl-gbdt:backend` stands in throughout. |
| `cl-gbdt:booster` | The same shape as `dataset` above: every trained booster is one, and no functional test names the class -- `create-booster` and `train` hand callers an opaque instance. `tests/handle.lisp` builds a bare `cl-gbdt:booster` directly with `cl-gbdt:make-handle` in nine `deftest` forms. Four read `booster-training-set` or `booster-best-iteration` straight back: `booster-retains-training-set` (TRAINING-SET), `booster-retains-best-iteration` (BEST-ITERATION), `booster-best-iteration-defaults-to-nil` and `booster-best-iteration-is-writable-after-construction` (BEST-ITERATION again, the second one twice over, to prove the writer replaces a constructor value too). The other five -- `resolve-best-num-iteration-reads-the-boosters-best-iteration`, `-signals-without-a-best-iteration`, `-passes-non-best-values-through`, `reject-best-num-iteration-signals-for-best` and `-passes-everything-else-through` -- build a bare booster the same way but read neither accessor, calling the internal `%resolve-best-num-iteration`/`%reject-best-num-iteration` helpers instead. `tests/protocol.lisp`'s `bare-booster` helper builds one the same way again for `a-fallback-never-widens-its-generic-to-accept-any-keyword`. Unlike `dataset`, nothing `typep`s the bare class by name here -- but every one of these constructions passes `'cl-gbdt:booster` itself as `make-handle`'s CLASS-NAME, the stronger fact a `typep` assertion would only restate, and all of it at layer 1, with a mock or bare backend standing in for a real one. |
| `cl-gbdt:training-series` | The class `training-report-series` holds a list of, one per metric per dataset; functional tests read instances back through `training-series-index`/`-name`/`-metric`/`-values` extensively (`tests/functional/training-report.lisp` and `-early-stopping.lisp` among others) but never construct one directly or `typep` against the class itself. `tests/training-report.lisp` does, at layer 1: `training-series-reports-what-it-was-built-from` builds one (through `make-training-series`, see that row below) with all four keywords distinct and asserts all four accessors -- INDEX, NAME, METRIC and VALUES -- return exactly what was given; `training-series-name-is-nil-when-unnamed` and `training-series-values-may-hold-nil` cover the two documented NIL cases on top of that. No shared library is any part of it -- a `training-series` is data built from keyword arguments and read back. |
| `cl-gbdt:make-training-series` | Read by the row above: `tests/training-report.lisp`'s `%series` helper is a thin wrapper over this constructor, and `training-series-reports-what-it-was-built-from`, `training-series-name-is-nil-when-unnamed` and `training-series-values-may-hold-nil` all call it through `%series`, then read every keyword back off the instance it returns; `training-report-prints-readably-enough-to-debug` builds one the same way to embed in a report. The only other caller anywhere is `src/training/history.lisp`'s `training-report-from-history`, called from both backends' `train` -- so every functional run that trains with metrics configured calls this too, just never by name. |
| `cl-gbdt:make-training-report` | `tests/training-report.lisp` calls it directly in four of its seven `deftest` forms. Three read every keyword back off the instance: `training-report-reports-what-it-was-built-from` (SERIES and NUM-ROUNDS), `training-report-early-stopping-slots-round-trip` (BEST-ITERATION, BEST-SCORE and EARLY-STOPPED-P all three) and `training-report-phase-3b-slots-are-nil` (the same three defaulting to NIL). The fourth, `training-report-prints-readably-enough-to-debug`, reads no accessor at all -- it builds one only to `princ-to-string` it and `search` the printed text for the series count and the round count, exercising `print-object` rather than the constructor's keywords. The only other caller is `src/training/history.lisp`'s `training-report-from-history`, `train`'s own helper on both backends; every functional run that reads `train`'s secondary value exercises that path instead, without ever naming this constructor. |
| `cl-gbdt:resolve-and-load-library` | `tests/library.lisp` calls it directly in all three of its `deftest` forms, each omitting a different argument to isolate a different stage of the search order: `resolve-and-load-library-honors-explicit-path` supplies a `:path` that does not exist (alongside an unset `:env-var` and a `:directory`/`:pattern` that match nothing), proving `:path` is tried first and reported as such; `resolve-and-load-library-env-var-is-strict` supplies no `:path` at all and instead sets a real environment variable to a nonexistent path, proving a bad `:env-var` signals `backend-library-not-found` rather than falling through to `:directory` or the system search; `resolve-and-load-library-falls-through-an-absent-directory` supplies neither `:path` nor a set `:env-var`, proving the remaining two stages -- an unmatched `:directory`, then CFFI's own system search -- are both tried and both fail. Together the three exercise every stage of the discovery order and both failure conditions (`backend-library-load-failed`, `backend-library-not-found`), all without `cffi:load-foreign-library` ever succeeding -- the same file, and the same deliberate non-loading, that `backend-library-load-failed`'s and `backend-library-not-found-searched`'s own rows above cite as their evidence. Both backends' `initialize-backend` call it for real, with real vendored paths, on every functional run -- the success branch this file's tests never reach -- but neither `classes.lisp` names `resolve-and-load-library` in a form any functional test file itself contains; the call is a layer inside `initialize-backend`, which the functional suite reaches only through `open-backend`. |
| `cl-gbdt:make-handle` | Stores POINTER, BACKEND and KIND without dereferencing or otherwise interpreting any of them -- it builds a CLOS instance and attaches a finalizer, nothing more. The rule that a fabricated condition proves nothing about a signal path does not disqualify this evidence: that rule catches a test that constructs the RESULT directly, skipping the code that decides whether to produce it at all (`missing-foreign-symbols` is the model case -- `(error 'missing-foreign-symbols ...)` never runs `initialize-backend`'s own judgment about whether to signal). Here the whole function runs, unmodified; a test merely supplies a POINTER and a BACKEND, both of which `make-handle`'s own contract treats as opaque. `tests/handle.lisp` calls it directly, with `(cffi:make-pointer 1)` or `2` and either the keyword `:mock` or a real (but library-less) `cl-gbdt:backend`, in every one of its relevant `deftest` forms; `tests/protocol.lisp`'s `bare-booster` and `a-handle-dispatching-generic-names-the-handles-backend` do too. No backend file calls `make-handle` itself at all: `clgrep-search` for a literal `(make-handle ...)` call across `src/` finds none outside `handle.lisp`'s own `defun` -- every mention of the name in `lightgbm/api.lisp`, `lightgbm/protocol.lisp`, `xgboost/api.lisp` and `xgboost/protocol.lisp` is docstring prose, not a call. The only real caller anywhere is `with-pointer-ownership`'s `take-ownership`, itself proven the same way (see that row below). |
| `cl-gbdt:release-handle` | FREE-FUNCTION is exactly what its own docstring calls it: "a function of one argument, HANDLE's pointer" -- an opaque callback `release-handle` invokes once and never inspects. A test supplying a counting closure instead of a real deallocator is an instance of that documented argument type, not a fabrication of the result: `release-handle`'s own code (call FREE-FUNCTION once, mark the handle released, cancel the finalizer) runs completely unmodified either way. `tests/handle.lisp`'s `release-handle-frees-exactly-once` and `release-handle-second-call-is-a-no-op` call it directly, proving the exactly-once and idempotent-second-call halves of its contract; `tests/handle-ownership.lisp`'s and `tests/protocol.lisp`'s own `drop` helpers call it too, to release their fixtures. Real backend code reaches it eight times, from four functions: `lightgbm/api.lisp`'s and `xgboost/api.lisp`'s `free-dataset` and `free-booster` each call it twice, once per branch of their open-backend/closed-backend `if`, always with a real backend-specific deallocator as FREE-FUNCTION. Both backends' `train` reaches the same release transitively, through the Layer 1 `free-booster` on its own failure-cleanup path, not through `release-handle` directly. |
| `cl-gbdt:with-pointer-ownership` | Seven dedicated `deftest` forms in `tests/handle-ownership.lisp` prove every documented clause of its contract directly, by name: `taking-ownership-suppresses-the-free` (a body that takes ownership leaves the pointer unfreed), `a-body-that-signals-frees-the-pointer` (a signalling body still frees), `an-error-from-the-free-function-does-not-replace-the-original` (policy section 10 -- a failing FREE-FUNCTION cannot mask the body's own condition), `the-bodys-values-are-the-forms-values` (multiple values pass through), `a-signal-after-ownership-moved-does-not-free`, `the-pointer-form-is-evaluated-once`, and `a-body-that-never-takes-ownership-frees-on-a-normal-return`. Three of the seven -- `a-body-that-signals-frees-the-pointer`, `an-error-from-the-free-function-does-not-replace-the-original` and `a-body-that-never-takes-ownership-frees-on-a-normal-return` -- never call the file's `test-backend` helper at all: each wraps a body that either signals immediately or returns a bare keyword without calling TAKE-OWNERSHIP, so the free-vs-keep decision these three exist to prove runs with no backend, real or fake, anywhere in the picture. The other four use `(make-instance 'cl-gbdt:backend :name :ownership-test)` only because their bodies call TAKE-OWNERSHIP, which needs a BACKEND to hand `make-handle`, not because the macro's own contract requires one. Every real call site is inside a backend's own `api.lisp`: `create-dataset`, `create-booster` and `load-model` in both `lightgbm/api.lisp` and `xgboost/api.lisp`, and `xgboost/api.lisp`'s `slice-model` too -- seven call sites in all. Neither `protocol.lisp` file calls this macro anywhere; both only mention its name in comments and docstrings, one of which (`lightgbm/protocol.lisp`'s `train`) describes the opposite of calling it: "Where the old body's `with-pointer-ownership` freed a RAW pointer, this frees the handle." |
| `cl-gbdt:backend` | The class every concrete backend -- `lightgbm-backend`, `xgboost-backend`, and every mock in the test suite -- inherits from, holding NAME, LIBRARY-PATH, CAPABILITIES, VERSION and OPENP. Three layer 1 files instantiate it directly, `(make-instance 'cl-gbdt:backend :name ...)`, not merely a subclass: `tests/protocol.lisp`'s `bare-backend`, `tests/library.lisp`'s `%test-backend`, and `tests/handle-ownership.lisp`'s `test-backend` -- stronger evidence of the class itself than `cl-gbdt:dataset`/`cl-gbdt:booster` get above, which are only ever built as instances of a NAMED SUBCLASS via `make-handle`'s CLASS-NAME argument. None of the three ever calls `initialize-backend` on it, so no library is ever touched, but what each does with the bare instance afterward differs, and only one of the three actually reads an accessor back. `test-backend` (`handle-ownership.lisp`) is only ever handed to `make-handle`, stored opaquely on a handle and never read back through any accessor of its own. `%test-backend` (`library.lisp`) is read once, internally, by `%resolve-library-path`'s `(backend-name backend)`, which becomes the `backend-library-not-found` or `backend-library-load-failed` it signals -- but none of that file's three tests reads BACKEND back off the resulting condition, so even this indirect exercise goes unconfirmed there. `bare-backend` (`protocol.lisp`) is the one place `backend-name` is actually proven: every fallback method in `src/protocol.lisp` reads it, directly or through `(handle-backend dataset)`, to build `backend-methods-not-loaded`'s `:backend` slot, and two tests read that slot back -- `a-backend-dispatching-generic-says-which-system-to-load`'s `(eq :lightgbm (cl-gbdt:backend-error-backend caught))` and `a-handle-dispatching-generic-names-the-handles-backend`'s identical assertion, reached through a handle instead. That is the one reader this row can claim proof for; the class's other exported accessors -- `backend-open-p` (not `backend-openp`, which is not exported), `backend-library-path`, `backend-capabilities` and `backend-version` -- are never read back by any of the three files. |
| `cl-gbdt:find-backend-class` | Pure hash-table lookup -- `(gethash name *backend-classes*)` -- with no library involved on any path, by any backend. `tests/backend.lisp`'s `registry-resolves-registered-backend` calls it directly, twice: `(eq 'mock-backend (cl-gbdt:find-backend-class :mock))` for a name `register-backend` (see that row below) registered moments earlier in the same file, and `(null (cl-gbdt:find-backend-class :no-such-backend))` for one that was never registered. `open-backend` also calls it, for real, as the first step of every functional test that opens a backend, but no functional test reads ITS return value back directly -- only `open-backend`'s eventual success or `unknown-backend` failure. The direct layer 1 assertions above are what actually prove the lookup's own contract; `open-backend`'s reliance on it is a second, weaker witness this row does not need. |
| `cl-gbdt:probe-foreign-symbols` | `tests/backend.lisp`'s `probe-foreign-symbols-detects-missing` and `probe-foreign-symbols-honors-library-keyword` call it directly, against libc's real, always-loaded `"strlen"` and two fabricated names, proving both documented halves of its contract -- a present symbol resolves, an absent one is reported, and a LIBRARY keyword genuinely reaches `cffi:foreign-symbol-pointer` rather than being dropped (an unregistered designator signals rather than falling back to a global search) -- real CFFI symbol resolution running to completion, the same shape `call-with-foreign-matrix`'s row above already stands on: CFFI/SBCL machinery that runs identically whether or not LightGBM or XGBoost is even installed, since libc needs neither. Both backends' `initialize-backend` also call it for real, with the real vendored library and `*required-symbols*`, on every functional `open-backend` -- proving, for the actual shipped libraries specifically, that every one of *their* required C names resolves, which the two layer 1 tests (`"strlen"` and two fabricated names) cannot speak to since neither vendored library is ever opened there. No functional test reads its return value back directly; `initialize-backend` only branches on whether it is NIL. |
| `cl-gbdt:register-backend` | Pure hash-table mutation -- `(setf (gethash name *backend-classes*) class-name)` -- with no library involved. Called directly at layer 1 four times across two files: `tests/backend.lisp`'s `(cl-gbdt:register-backend :mock 'mock-backend)` and `(cl-gbdt:register-backend :failing 'failing-backend)`, and `tests/handle.lisp`'s `(cl-gbdt:register-backend :handle-test '%mock-backend)` and `(cl-gbdt:register-backend :other-handle-test '%other-mock-backend)`. Its effect -- that the association was actually stored -- is what `registry-resolves-registered-backend` reads back through `find-backend-class` (see that row above), moments after `tests/backend.lisp`'s own two calls run. Both backends' own `classes.lisp` also call it, unconditionally, the instant `cl-gbdt/lightgbm` or `cl-gbdt/xgboost` loads (`(register-backend :lightgbm 'lightgbm-backend)`, `(register-backend :xgboost 'xgboost-backend)`) -- without which `open-backend` could never find either class and every functional test would fail at the first `unknown-backend` -- but that is a load-time side effect nothing calls during a test's own execution, not a wrapper a test reaches through. |

## Exempt: reached through a published wrapper

Every functional run executes it, but through another published symbol the tests do name. A test
naming this one directly would exercise the same code by a longer route.

A row belongs here only when the chain of wrappers it names bottoms out in a symbol the
functional suite genuinely covers -- directly, or transitively through another row in any
section of this file -- otherwise neither end of the chain is proven and every link in it is a
gap. `handle-pointer`'s row below is the reason this is "any section" and not "this section":
its second path runs through `release-handle`, whose own row sits under "Exempt: proven without
a shared library" rather than here, and that path is exactly as sound as the first.

| Symbol | The wrapper that reaches it |
|---|---|
| `cl-gbdt:handle-live-pointer` | Called at the top of every operation that reads an existing live handle, to get its pointer and refuse an already-released or backend-closed one, before any foreign call: `lightgbm/api.lisp`'s `update-one-iteration`, `predict`, `save-model` and `feature-importance`, and `dataset-num-rows`/`dataset-num-features` (paired with `%check-object-class`), and the equivalent functions in `xgboost/api.lisp`. Functional tests reach it constantly through those covered operations without a single one naming `handle-live-pointer` itself -- `lightgbm-api.lisp`'s `lightgbm-api-round-trip` alone calls `dataset-num-rows`, `dataset-num-features` and `predict` (twice) in one `deftest`; `sparse-input.lisp` and `missing-value.lisp` call `dataset-num-rows`/`dataset-num-features` repeatedly across their fixtures; `lightgbm-api-free-dataset-releases-the-handle` and `lightgbm-api-operation-after-close-backend-signals-backend-not-open` provoke its two error branches for real, through `dataset-num-rows`, against an actually-freed and an actually-closed handle. `tests/handle.lisp` additionally asserts its three-way contract directly, at layer 1: `live-pointer-on-live-handle-returns-the-pointer`, `live-pointer-on-released-handle-signals` and `live-pointer-on-handle-whose-backend-is-closed-signals-backend-not-open`. |
| `cl-gbdt:handle-backend` | Read inside `handle-live-pointer`'s own body: once, unconditionally, to ask `backend-open-p` -- so every successful call in that row above reads it exactly once -- and a second time only on the failure branch, to build `backend-not-open`'s BACKEND value when the answer was NIL. `%check-handle-kind` reads it a third way, comparing `(backend-name (handle-backend object))` against the backend a caller-supplied handle claims to belong to -- exercised for real by `tests/functional/evaluation.lisp`'s `evaluation-layer-1-entry-points-reject-the-other-backends-handles`, which hands each backend's real `booster-eval`/`booster-eval-names`/`evaluate-one-iteration` the other backend's real handle. No test, layer 1 or functional, calls `handle-backend` itself; `handle-live-pointer` carries it into every functional run instead. `handle-live-pointer` is not itself covered -- it has its own row in this same section, immediately above -- but that row's chain bottoms out in a symbol the suite genuinely covers, transitively, through `predict`, `dataset-num-rows` and the rest, which is what makes this row's own chain sound in turn. |
| `cl-gbdt:handle-pointer` | Read once inside `handle-live-pointer`'s own body -- its final clause, once release and backend-open have both been checked, is `(handle-pointer handle)` -- and once inside `release-handle`, which frees a handle's pointer without re-checking liveness (freeing an already-released handle is meant to be a safe no-op, not a second `released-handle-error`). Both routes lead to symbols the functional suite genuinely covers: `handle-live-pointer` transitively, through `predict`/`dataset-num-rows`/etc. (see that row above); `release-handle` transitively, through `free-dataset`/`free-booster`, both named directly in essentially every `tests/functional/*.lisp` fixture's cleanup. `handle-live-pointer`'s own docstring explains why nothing should call this reader directly instead: "Every operation that reaches into a backend's C API must read the pointer through this function, never `handle-pointer` directly." |
| `cl-gbdt:initialize-backend` | `open-backend` calls it, unconditionally, as its whole initialization step: `(let ((backend (make-instance class-name :name name))) (initialize-backend backend :path path) (setf (backend-openp backend) t) backend)`. Essentially every functional `deftest` in `tests/functional/` calls `cl-gbdt:open-backend` directly, which is what runs the concrete method -- `lightgbm-backend`'s or `xgboost-backend`'s -- that actually resolves and loads the real vendored library, probes `*required-symbols*` and `*optional-symbols*`, and (on XGBoost) reads and checks the real version, all for real on every one of those calls. `tests/backend.lisp`'s `initialization-failure-propagates` also calls `cl-gbdt:initialize-backend` directly, but only on a `failing-backend` -- a `mock-backend` subclass that defines no `initialize-backend` method of its own, only overriding `fail-with`'s initform to `t` -- so the inherited method, `mock-backend`'s, is what actually runs: `(when (mock-backend-fail-with backend) (error 'cl-gbdt:backend-library-not-found ...))`, unconditionally true for this subclass and so unconditionally erroring before touching a library at all. It proves the generic function dispatches (to the inherited method) and that a failure signal propagates through it undisturbed, not any part of the library-loading contract only the real backends implement. That contract -- everything from `resolve-and-load-library` down -- is proven only through `open-backend`, and only there. |
| `cl-gbdt:shutdown-backend` | `close-backend` calls it, unconditionally when the backend is open: `(when (backend-openp backend) (shutdown-backend backend) (setf (backend-openp backend) nil))`. Nearly every functional `deftest` calls `cl-gbdt:close-backend` in its cleanup, running the concrete method that actually closes the real vendored library via `cffi:close-foreign-library`. No test, layer 1 or functional, calls `cl-gbdt:shutdown-backend` directly -- unlike `initialize-backend` above, even `tests/backend.lisp`'s mock never has its own `shutdown-backend` method exercised except through `close-backend`: `close-backend-is-idempotent` reads `mock-backend-shutdown-count` back, but only after calling `close-backend` twice, never `shutdown-backend` itself. |
| `cl-gbdt:*known-capabilities*` | Read inside `backend-supports-p`'s own body, twice: once in the `member` gate (`(unless (member capability *known-capabilities*) (error 'unknown-capability ...))`) and, on that failure branch, again to build `unknown-capability`'s `:known` initarg. `backend-supports-p` is called directly, with real capability keywords, in exactly eleven of the sixteen `tests/functional/*.lisp` files -- counted across all three spellings the public surface offers, `cl-gbdt:backend-supports-p`, `cl-gbdt/lightgbm:backend-supports-p` and `cl-gbdt/xgboost:backend-supports-p`, which name the identical symbol object (`src/lightgbm/all.lisp` and `src/xgboost/all.lisp` both `:import-from` it from `cl-gbdt/src/backend` unchanged rather than each defining their own, unlike `predict`/`dataset-num-rows`), verified file by file: `categorical-features.lisp`, `custom-evaluation.lisp`, `custom-objective.lisp`, `early-stopping.lisp`, `evaluation.lisp`, `missing-value.lisp`, `prediction-shape.lisp`, `sparse-input.lisp` and `training-report.lisp` under the `cl-gbdt:` spelling, plus `lightgbm-api.lisp`'s and `xgboost-api.lisp`'s `the-backend-package-publishes-what-a-standalone-caller-needs` (one call each) under `cl-gbdt/lightgbm:` and `cl-gbdt/xgboost:` respectively. Every one of those eleven files' calls runs the `member` check against `*known-capabilities*` for real. No test, layer 1 or functional, reads the variable's own value directly: the only reader that could, `unknown-capability-known`, is itself unproven (see that row above), so even the failure branch's use of `*known-capabilities*` is unconfirmed beyond "the report contains at least one registered name". |

## Exempt: an implementor's tool

Published for someone writing a backend, not for someone training a model, and with no test --
layer 1 or functional -- naming it anywhere: proving it would mean writing backend-authoring
code that a caller who only trains and predicts never has occasion to write, real or fake.

| Symbol | Who it is for |
|---|---|
| `cl-gbdt:write-foreign-sequence` | Copies a Lisp sequence into a foreign array through a caller-supplied coercer. Every real call site is inside a backend's own `native.lisp` -- LightGBM's `%set-dataset-field` and XGBoost's `%set-info-field` and `%set-group-field` -- and no test, layer 1 or functional, calls it by name; a caller who passes an array or a label vector to `make-dataset` reaches it without ever naming it, and every functional run that attaches a label exercises it that way. |
| `cl-gbdt:with-foreign-float-traps-masked` | Wraps the entire body of every `defmethod` in either backend's `protocol.lisp` and `classes.lisp`, and every exported `defun` in their `api.lisp` and `native.lisp` -- `tools/ci/check-float-traps.lisp` fails the build if one of them is missing it. A caller who trains or predicts never writes this macro; they get its effect automatically on every call into either library, and `CLAUDE.md` documents it as a rule backends themselves must follow. It has no dedicated test of its own: the SBCL-vs-C floating-point trap mismatch it corrects for -- confirmed by a real, once-uncaught `floating-point-invalid-operation` inside XGBoost's `multi:softprob` softmax on x86-64 -- is a property of the platform's trap configuration around a foreign call, not a value a layer 1 test could construct without one. |

## Exempt: an abstract condition its subclasses prove

A parent condition nothing signals directly. Functional tests catch its concrete subclasses, which
is the only way it is ever reached, so a test naming the parent would assert nothing the subclass
tests do not.

A row belongs here only when nothing signals the type itself. A parent that IS signalled somewhere
is a gap, not an abstraction.

| Symbol | Which subclasses prove it |
|---|---|
| `cl-gbdt:backend-error` | Nothing signals it directly -- `clgrep-search` across `src/` for `backend-error` finds only its own `define-condition`, its `backend-error-backend` reader, and every subclass's own `define-condition` naming it as their supertype. `src/conditions.lisp` defines it as the parent every backend-side failure inherits. Functional tests catch three of its concrete subclasses directly: `capability-unavailable` (e.g. `categorical-features-without-the-capability-signals`, `missing-value-without-the-capability-signals`, `sparse-input-without-the-capability-signals`, and their siblings across `tests/functional/`), `foreign-call-error` (e.g. `lightgbm-api-make-dataset-wrong-length-label-signals-without-leaking`), and `backend-not-open` (e.g. `lightgbm-api-operation-after-close-backend-signals-backend-not-open` and its ten siblings across both backends). |

## Exempt: only reachable through a broken shared library

Narrower than "proven without a shared library" above, and not to be confused with it: the
symbol's true signal path needs a real shared library -- just a broken one, missing a function
this project requires after otherwise loading successfully. Neither vendored library,
`liblightgbm.so` or `libxgboost.so`, is ever in that state, and nothing in this suite builds or
loads a deliberately incomplete one, so no functional test -- real or hypothetically
constructed -- has ever reached this path. That is a claim about reachability, not a claim that
a fabricated-condition test stands in for it: where one is cited below, it is offered only for
what it actually proves, the condition object's own report contract in isolation, not the real
`initialize-backend` signal path, which nothing here exercises.

`cl-gbdt:backend-library-load-failed` (see "Exempt: proven without a shared library" above) is
NOT the same shape despite a surface resemblance ("only happens when a load fails"): when IT
fires, `cffi:load-foreign-library` has already rejected the candidate path, so zero bytes of any
shared library are ever mapped into the process -- the heading above's claim holds literally.
Here, by contrast, a real library must already be mapped in and successfully answering symbol
queries before this condition can fire at all.

| Symbol | Why the real signal path is unreachable here |
|---|---|
| `cl-gbdt:missing-foreign-symbols` | Signalled from both backends' `initialize-backend` when `probe-foreign-symbols` finds a `*required-symbols*` name unresolved in the library that was JUST loaded successfully -- true only of a version-mismatched library, which neither vendored `.so` is; `with-backend-library` always loads the real, matching one. `tests/conditions.lisp`'s `missing-foreign-symbols-reports-names` directly instantiates the condition, `(error 'cl-gbdt:missing-foreign-symbols :backend :lightgbm :names '("LGBM_GetMaxThreads" "LGBM_SetMaxThreads"))`, with no library loaded, and checks the printed report contains both names and the count `"2"`. That proves the condition's own report contract; it says nothing about the real path through `initialize-backend`, which no test, layer 1 or functional, exercises. |
| `cl-gbdt:missing-foreign-symbols-names` | Same absence of a real signal path as the row above. The same `missing-foreign-symbols-reports-names` test is the only place this reader is exercised, and only through a fabricated instance -- the same shape of evidence this file declines to accept for `foreign-call-error-code`, where the condition ALSO fires for real elsewhere and goes unread there. The difference here is that `missing-foreign-symbols` never fires for real anywhere in this suite, so there is no competing real-world instance for the fabricated one to fall short of; it is the only evidence this reader could have without this project shipping a deliberately broken second library. Within that limit, it does prove something: both names and the count `"2"` appear in the printed report, which only holds if this reader returns both. |
